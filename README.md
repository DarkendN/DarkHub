# DARKHUB
Bản frontend được dựng lại theo bố cục các ảnh mẫu: login đỏ/đen, dashboard mobile, modal tạo script và màn hình bảo vệ script.

Demo: `admin` / `password`.
Mỗi script tạo mới có mã riêng dạng `Dark-XXX-XXX-XXX-XXXX`.
Dữ liệu demo dùng localStorage, không phải backend bảo mật thật.
